const seed = {
  units: [
    { id: '01', status: 'occupied', rent: 10000, deposit: 20000, details: 'First floor, quiet side', amenities: 'Aircon, own CR', notes: 'Contract renewal soon', photo: '' },
    { id: '02', status: 'occupied', rent: 10000, deposit: 20000, details: 'First floor', amenities: 'Aircon, own CR', notes: '', photo: '' },
    { id: '03', status: 'occupied', rent: 10000, deposit: 20000, details: 'First floor', amenities: 'Aircon, own CR', notes: '', photo: '' },
    { id: '04', status: 'maintenance', rent: 10000, deposit: 20000, details: 'Second floor', amenities: 'Aircon, own CR', notes: 'Faucet repair', photo: '' },
    { id: '05', status: 'occupied', rent: 10000, deposit: 20000, details: 'Second floor', amenities: 'Aircon, own CR', notes: 'Contract expires Sep 15', photo: '' },
    { id: '06', status: 'occupied', rent: 10000, deposit: 20000, details: 'Second floor', amenities: 'Aircon, own CR', notes: '', photo: '' },
    { id: '07', status: 'occupied', rent: 10000, deposit: 20000, details: 'Second floor', amenities: 'Aircon, own CR', notes: 'Leaking faucet reported', photo: '' },
    { id: '08', status: 'occupied', rent: 10000, deposit: 20000, details: 'Third floor', amenities: 'Aircon, own CR', notes: '', photo: '' },
    { id: '09', status: 'available', rent: 10000, deposit: 20000, details: 'Third floor', amenities: 'Aircon, own CR', notes: 'Ready for move-in', photo: '' },
    { id: '10', status: 'available', rent: 10000, deposit: 20000, details: 'Third floor', amenities: 'Aircon, own CR', notes: '', photo: '' }
  ],
  tenants: [
    { id: 't1', name: 'Juan Dela Cruz', unit: '01', phone: '0917 123 4567', email: 'juan@example.com', emergency: 'Maria Dela Cruz · 0918 111 2222', moveIn: '2026-01-15', moveOut: '', contractStart: '2026-01-15', contractEnd: '2027-01-15', status: 'Active', notes: '' },
    { id: 't2', name: 'Maria Santos', unit: '02', phone: '0917 234 5678', email: 'maria@example.com', emergency: 'Rico Santos · 0918 222 3333', moveIn: '2026-02-01', moveOut: '', contractStart: '2026-02-01', contractEnd: '2027-02-01', status: 'Active', notes: '' },
    { id: 't3', name: 'Pedro Reyes', unit: '03', phone: '0917 345 6789', email: 'pedro@example.com', emergency: 'Ana Reyes · 0918 333 4444', moveIn: '2026-03-01', moveOut: '', contractStart: '2026-03-01', contractEnd: '2027-03-01', status: 'Active', notes: '' },
    { id: 't4', name: 'Ana Bautista', unit: '04', phone: '0917 456 7890', email: 'ana@example.com', emergency: 'Jose Bautista · 0918 444 5555', moveIn: '2026-01-20', moveOut: '', contractStart: '2026-01-20', contractEnd: '2027-01-20', status: 'Active', notes: '' },
    { id: 't5', name: 'Liza Garcia', unit: '05', phone: '0917 567 8901', email: 'liza@example.com', emergency: 'Nora Garcia · 0918 555 6666', moveIn: '2025-09-15', moveOut: '', contractStart: '2025-09-15', contractEnd: '2026-09-15', status: 'Active', notes: 'Discuss renewal' },
    { id: 't6', name: 'Carlo Mendoza', unit: '06', phone: '0917 678 9012', email: 'carlo@example.com', emergency: 'Mia Mendoza · 0918 666 7777', moveIn: '2026-01-10', moveOut: '', contractStart: '2026-01-10', contractEnd: '2027-01-10', status: 'Active', notes: '' },
    { id: 't7', name: 'Ramon Flores', unit: '07', phone: '0917 789 0123', email: 'ramon@example.com', emergency: 'Lara Flores · 0918 777 8888', moveIn: '2026-02-15', moveOut: '', contractStart: '2026-02-15', contractEnd: '2027-02-15', status: 'Active', notes: '' },
    { id: 't8', name: 'Nina Cruz', unit: '08', phone: '0917 890 1234', email: 'nina@example.com', emergency: 'Nico Cruz · 0918 888 9999', moveIn: '2026-03-10', moveOut: '', contractStart: '2026-03-10', contractEnd: '2027-03-10', status: 'Active', notes: '' }
  ],
  payments: [
    { id: 'p1', unit: '01', billing: 'August 2026', due: '2026-08-01', dueAmount: 10000, paid: 10000, date: '2026-08-01', method: 'GCash', reference: 'GC-001', status: 'Paid' },
    { id: 'p2', unit: '02', billing: 'August 2026', due: '2026-08-01', dueAmount: 10000, paid: 5000, date: '2026-08-03', method: 'Cash', reference: 'CS-002', status: 'Partial' },
    { id: 'p3', unit: '03', billing: 'August 2026', due: '2026-08-01', dueAmount: 10000, paid: 10000, date: '2026-08-01', method: 'GCash', reference: 'GC-003', status: 'Paid' },
    { id: 'p4', unit: '04', billing: 'August 2026', due: '2026-08-01', dueAmount: 10000, paid: 10000, date: '2026-08-01', method: 'Bank transfer', reference: 'BT-004', status: 'Paid' },
    { id: 'p5', unit: '05', billing: 'August 2026', due: '2026-08-01', dueAmount: 10000, paid: 0, date: '', method: '', reference: '', status: 'Unpaid' },
    { id: 'p6', unit: '06', billing: 'August 2026', due: '2026-08-01', dueAmount: 10000, paid: 10000, date: '2026-08-01', method: 'Cash', reference: 'CS-006', status: 'Paid' },
    { id: 'p7', unit: '07', billing: 'August 2026', due: '2026-08-01', dueAmount: 10000, paid: 5000, date: '2026-08-05', method: 'Cash', reference: 'CS-007', status: 'Partial' },
    { id: 'p8', unit: '08', billing: 'August 2026', due: '2026-08-01', dueAmount: 10000, paid: 10000, date: '2026-08-01', method: 'Bank transfer', reference: 'BT-008', status: 'Paid' }
  ],
  expenses: [{ id: 'e1', date: '2026-08-04', category: 'Electricity', description: 'Common area bill', amount: 4200, vendor: 'Meralco', method: 'Bank transfer', reference: 'MER-08', notes: '' }, { id: 'e2', date: '2026-08-08', category: 'Water', description: 'Property supply', amount: 1800, vendor: 'Maynilad', method: 'Cash', reference: 'W-08', notes: '' }, { id: 'e3', date: '2026-08-12', category: 'Repairs', description: 'Unit 04 faucet', amount: 1500, vendor: 'Mang Tony', method: 'Cash', reference: 'R-04', notes: '' }],
  maintenance: [{ id: 'm1', unit: '07', issue: 'Leaking faucet', reported: '2026-08-24', priority: 'High', assigned: 'Mang Tony', estimated: 1500, actual: 0, status: 'In Progress', completed: '' }, { id: 'm2', unit: '04', issue: 'Replace hallway light', reported: '2026-08-22', priority: 'Medium', assigned: 'Admin Juan', estimated: 650, actual: 0, status: 'Pending', completed: '' }],
  utilities: [{ id: 'u1', unit: '01', type: 'Water', previous: 120, current: 132, rate: 45, billing: 'August 2026', status: 'Unpaid' }, { id: 'u2', unit: '02', type: 'Electricity', previous: 820, current: 915, rate: 14, billing: 'August 2026', status: 'Paid' }]
};

const data = JSON.parse(localStorage.getItem('casaJavaData') || 'null') || seed;
const appContent = document.getElementById('appContent');
const breadcrumbCurrent = document.getElementById('breadcrumbCurrent');
const toast = document.getElementById('toast');
let currentView = 'dashboard';
const save = () => localStorage.setItem('casaJavaData', JSON.stringify(data));
const peso = value => `₱${Number(value || 0).toLocaleString('en-PH')}`;
const label = value => value ? value.charAt(0).toUpperCase() + value.slice(1) : '';
const escapeHtml = value => String(value ?? '').replace(/[&<>"']/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;' }[char]));
const statusPill = (value, tone = '') => `<span class="status-pill ${tone || String(value).toLowerCase().replaceAll(' ', '-')}">${escapeHtml(value)}</span>`;
const field = (name, title, value = '', type = 'text', required = false) => `<label class="form-field"><span>${title}</span><input name="${name}" type="${type}" value="${escapeHtml(value)}" ${required ? 'required' : ''}></label>`;
const select = (name, title, options, value = '') => `<label class="form-field"><span>${title}</span><select name="${name}">${options.map(option => `<option ${option === value ? 'selected' : ''}>${option}</option>`).join('')}</select></label>`;
function metric(title, value, meta, icon, tone = '') { return `<article class="metric-card ${tone}"><span class="metric-icon">${icon}</span><div class="metric-label">${title}</div><div class="metric-value">${value}</div><div class="metric-meta">${meta}</div></article>`; }
function pageHead(eyebrow, title, description, action = 'Add record') { return `<div class="page-heading"><div><p class="eyebrow">${eyebrow}</p><h1>${title}</h1><p class="page-description">${description}</p></div><button class="primary-button" data-add="${currentView === 'dashboard' ? 'units' : currentView}">＋ ${action}</button></div>`; }
function toolbar(placeholder = 'Search records...') { return `<div class="toolbar"><label class="search-box">⌕<input id="searchInput" placeholder="${placeholder}"></label><button class="filter-button" id="filterButton">Filter <span>⌄</span></button></div>`; }
function table(headers, rows) { return `<div class="table-scroll"><table class="table"><thead><tr>${headers.map(header => `<th>${header}</th>`).join('')}</tr></thead><tbody>${rows || `<tr><td colspan="${headers.length}" class="empty-note">No records found.</td></tr>`}</tbody></table></div>`; }
function actions(type, id) { return `<td class="row-actions"><button title="Edit" data-edit="${type}" data-id="${id}">✎</button><button title="Delete" data-delete="${type}" data-id="${id}">×</button></td>`; }
function unitTenant(unit) { return data.tenants.find(tenant => tenant.unit === unit.id)?.name || (unit.status === 'available' ? 'Available' : 'Unassigned'); }
function unitTable(list, compact = false) { return table(['Unit', 'Tenant', 'Status', 'Monthly rent', compact ? 'Payment' : 'Deposit', ''], list.map(unit => { const payment = data.payments.find(item => item.unit === unit.id && item.billing === 'August 2026'); return `<tr><td class="unit-cell">Unit ${unit.id}</td><td class="tenant-cell">${escapeHtml(unitTenant(unit))}</td><td>${statusPill(label(unit.status))}</td><td class="money">${peso(unit.rent)}</td><td>${compact ? statusPill(payment?.status || 'Unpaid') : peso(unit.deposit)}</td>${actions('units', unit.id)}</tr>`; }).join('')); }

function renderDashboard() {
  const occupied = data.units.filter(unit => unit.status === 'occupied').length;
  const collected = data.payments.reduce((sum, payment) => sum + Number(payment.paid || 0), 0);
  const outstanding = data.payments.reduce((sum, payment) => sum + Math.max(0, Number(payment.dueAmount || 0) - Number(payment.paid || 0)), 0);
  const expenses = data.expenses.reduce((sum, expense) => sum + Number(expense.amount || 0), 0);
  appContent.innerHTML = `<div class="view-section">${pageHead('Tuesday, August 25, 2026', 'Good morning, Admin.', 'Here is what is happening at Casa Java today.')}<div class="metrics-grid">${metric('Total units', data.units.length, 'Apartment capacity', '▦')}${metric('Occupied', occupied, `${Math.round(occupied / data.units.length * 100)}% occupancy rate`, '⌂', 'featured')}${metric('Available', data.units.filter(unit => unit.status === 'available').length, 'Ready for move-in', '○', 'accent')}${metric('Maintenance', data.units.filter(unit => unit.status === 'maintenance').length, 'Units needing attention', '⌁')}${metric('Rent collected', peso(collected), `${data.payments.filter(payment => payment.status === 'Paid').length} paid records`, '₱', 'featured')}${metric('Outstanding', peso(outstanding), 'Partial or unpaid rent', '!', 'warning')}${metric('Pending tasks', data.maintenance.filter(item => item.status !== 'Completed').length, 'Open work orders', '☷')}${metric('Net income', peso(collected - expenses), 'After recorded expenses', '↗', 'accent')}</div><div class="content-grid"><section class="panel"><div class="panel-header"><div><h2 class="panel-title">Reminders</h2><p class="panel-subtitle">Items that need your attention</p></div><button class="text-button" data-view-link="notifications">View all →</button></div><div class="activity-list">${reminders().slice(0, 4).map(item => `<div class="activity"><span class="activity-icon ${item.type}">${item.icon}</span><div class="activity-copy"><strong>${item.title}</strong><span>${item.detail}</span></div><span class="activity-time">${item.time}</span></div>`).join('') || '<p class="empty-note">All clear for now.</p>'}</div></section><section class="panel"><div class="panel-header"><div><h2 class="panel-title">Occupancy</h2><p class="panel-subtitle">Current unit distribution</p></div></div><div class="occupancy-wrap"><div class="donut" style="background: conic-gradient(var(--green) 0 ${occupied / data.units.length * 100}%, #dcece4 ${occupied / data.units.length * 100}% 100%)"><div class="donut-center"><strong>${Math.round(occupied / data.units.length * 100)}%</strong><span>occupied</span></div></div><div class="occupancy-legend"><div class="legend-row"><span class="legend-name"><i class="legend-dot"></i>Occupied</span><strong>${occupied} units</strong></div><div class="legend-row"><span class="legend-name"><i class="legend-dot available"></i>Available</span><strong>${data.units.filter(unit => unit.status === 'available').length} units</strong></div><div class="legend-row"><span class="legend-name"><i class="legend-dot" style="background:var(--orange)"></i>Maintenance</span><strong>${data.units.filter(unit => unit.status === 'maintenance').length} units</strong></div></div></div></section></div><section class="panel table-panel"><div class="panel-header"><div><h2 class="panel-title">Unit snapshot</h2><p class="panel-subtitle">Payment status for August 2026</p></div><button class="text-button" data-view-link="units">Manage units →</button></div>${unitTable(data.units.slice(0, 5), true)}</section></div>`;
}
function renderUnits() { appContent.innerHTML = `<div class="view-section">${pageHead('Property directory', 'Units', 'Manage unit details, amenities, photos, and availability.')}${toolbar('Search unit or tenant...')}<section class="panel table-panel" id="listPanel">${unitTable(data.units)}</section></div>`; setupSearch('units'); }
function renderTenants() { appContent.innerHTML = `<div class="view-section">${pageHead('Resident records', 'Tenants', 'Profiles, contracts, contacts, and tenant history.')}${toolbar('Search tenant or unit...')}<section class="panel table-panel" id="listPanel">${table(['Tenant', 'Unit', 'Contact', 'Contract expires', 'Status', ''], data.tenants.map(tenant => `<tr><td class="unit-cell">${escapeHtml(tenant.name)}</td><td>Unit ${tenant.unit}</td><td class="tenant-cell">${escapeHtml(tenant.phone)}</td><td>${tenant.contractEnd || '—'}</td><td>${statusPill(tenant.status)}</td>${actions('tenants', tenant.id)}</tr>`).join(''))}</section></div>`; setupSearch('tenants'); }
function renderPayments() { const paid = data.payments.reduce((sum, item) => sum + Number(item.paid || 0), 0); const due = data.payments.reduce((sum, item) => sum + Number(item.dueAmount || 0), 0); appContent.innerHTML = `<div class="view-section">${pageHead('Cash flow', 'Payments', 'Billing periods, receipts, balances, and collection history.') }<div class="metrics-grid">${metric('Collected this month', peso(paid), 'Received payments', '₱', 'featured')}${metric('Outstanding balance', peso(due - paid), 'Partial or unpaid', '!', 'warning')}${metric('Collection rate', `${due ? Math.round(paid / due * 100) : 0}%`, 'Against amount due', '↗', 'accent')}${metric('Overdue records', data.payments.filter(item => item.status === 'Unpaid').length, 'Needs follow-up', '◷')}</div>${toolbar('Search payment, unit, or reference...')}<section class="panel table-panel" id="listPanel">${table(['Unit', 'Billing period', 'Due', 'Paid', 'Balance', 'Status', ''], data.payments.map(payment => `<tr><td class="unit-cell">Unit ${payment.unit}</td><td>${payment.billing}</td><td class="money">${peso(payment.dueAmount)}</td><td class="money">${peso(payment.paid)}</td><td class="money">${peso(Math.max(0, payment.dueAmount - payment.paid))}</td><td>${statusPill(payment.status)}</td>${actions('payments', payment.id)}</tr>`).join(''))}</section></div>`; setupSearch('payments'); }
function renderExpenses() { const total = data.expenses.reduce((sum, item) => sum + Number(item.amount || 0), 0); appContent.innerHTML = `<div class="view-section">${pageHead('Property costs', 'Expenses', 'Track vendors, receipts, categories, and monthly operating costs.') }<div class="metrics-grid">${metric('August expenses', peso(total), `${data.expenses.length} records`, '◌')}${metric('Largest category', expenseCategory(), 'Highest recorded spend', '↗', 'accent')}</div>${toolbar('Search expense or vendor...')}<section class="panel table-panel" id="listPanel">${table(['Date', 'Category', 'Description', 'Vendor', 'Amount', ''], data.expenses.map(expense => `<tr><td>${expense.date}</td><td>${statusPill(expense.category, 'info-pill')}</td><td>${escapeHtml(expense.description)}</td><td class="tenant-cell">${escapeHtml(expense.vendor)}</td><td class="money">${peso(expense.amount)}</td>${actions('expenses', expense.id)}</tr>`).join(''))}</section></div>`; setupSearch('expenses'); }
function expenseCategory() { const totals = {}; data.expenses.forEach(item => { totals[item.category] = (totals[item.category] || 0) + Number(item.amount); }); const top = Object.entries(totals).sort((a, b) => b[1] - a[1])[0]; return top ? peso(top[1]) : peso(0); }
function renderMaintenance() { appContent.innerHTML = `<div class="view-section">${pageHead('Work orders', 'Maintenance', 'Move each repair from reported to completed.')}${toolbar('Search issue, unit, or assignee...')}<section class="panel table-panel" id="listPanel">${table(['Unit', 'Issue', 'Priority', 'Assigned', 'Cost', 'Status', ''], data.maintenance.map(item => `<tr><td class="unit-cell">Unit ${item.unit}</td><td>${escapeHtml(item.issue)}</td><td>${statusPill(item.priority, item.priority === 'High' ? 'warning-pill' : 'info-pill')}</td><td>${escapeHtml(item.assigned)}</td><td class="money">${peso(item.actual || item.estimated)}</td><td>${statusPill(item.status)}</td>${actions('maintenance', item.id)}</tr>`).join(''))}</section></div>`; setupSearch('maintenance'); }
function renderUtilities() { appContent.innerHTML = `<div class="view-section">${pageHead('Consumption and billing', 'Utilities', 'Calculate water and electricity bills from meter readings.')}${toolbar('Search unit or utility type...')}<section class="panel table-panel" id="listPanel">${table(['Unit', 'Type', 'Billing period', 'Consumption', 'Rate', 'Amount due', 'Status', ''], data.utilities.map(item => `<tr><td class="unit-cell">Unit ${item.unit}</td><td>${item.type}</td><td>${item.billing}</td><td class="money">${Number(item.current) - Number(item.previous)}</td><td class="money">${peso(item.rate)}</td><td class="money">${peso((Number(item.current) - Number(item.previous)) * Number(item.rate))}</td><td>${statusPill(item.status)}</td>${actions('utilities', item.id)}</tr>`).join(''))}</section></div>`; setupSearch('utilities'); }
function renderReports() { const income = data.payments.reduce((sum, item) => sum + Number(item.paid || 0), 0); const expenses = data.expenses.reduce((sum, item) => sum + Number(item.amount || 0), 0); appContent.innerHTML = `<div class="view-section">${pageHead('Performance', 'Reports', 'Monthly income, expense, occupancy, and property health.') }<div class="report-actions"><button class="secondary-button" data-export="csv">⇩ Export CSV</button><button class="secondary-button" data-export="pdf">▣ Print / PDF</button></div><div class="metrics-grid">${metric('Gross rent', peso(income), 'Collected payments', '₱', 'featured')}${metric('Expenses', peso(expenses), 'Recorded operating costs', '◌')}${metric('Net income', peso(income - expenses), 'Income less expenses', '↗', 'accent')}${metric('Occupancy', `${Math.round(data.units.filter(unit => unit.status === 'occupied').length / data.units.length * 100)}%`, `${data.units.length} total units`, '⌂')}</div><section class="panel report-chart"><div class="panel-header"><div><h2 class="panel-title">Monthly summary</h2><p class="panel-subtitle">Income and expenses at a glance</p></div></div><div class="bar-chart"><div style="height:${Math.max(12, income / 1000)}%"><span>${peso(income)}</span></div><div class="expense-bar" style="height:${Math.max(12, expenses / 1000)}%"><span>${peso(expenses)}</span></div></div><div class="chart-labels"><span>Income</span><span>Expenses</span></div></section></div>`; }
function renderNotifications() { appContent.innerHTML = `<div class="view-section">${pageHead('Attention needed', 'Notifications', 'Rent, contract, maintenance, and utility reminders.') }<section class="panel"><div class="activity-list">${reminders().map(item => `<div class="activity"><span class="activity-icon ${item.type}">${item.icon}</span><div class="activity-copy"><strong>${item.title}</strong><span>${item.detail}</span></div><span class="activity-time">${item.time}</span></div>`).join('') || '<p class="empty-note">No notifications.</p>'}</div></section></div>`; }
function reminders() { const result = []; data.payments.filter(item => item.status !== 'Paid').forEach(item => result.push({ type: 'warning', icon: '!', title: `Unit ${item.unit} has ${item.status.toLowerCase()} rent`, detail: `${peso(item.dueAmount - item.paid)} outstanding for ${item.billing}`, time: 'Action needed' })); data.tenants.filter(item => item.contractEnd === '2026-09-15').forEach(item => result.push({ type: 'info', icon: 'i', title: `${item.name}'s contract expires soon`, detail: `Unit ${item.unit} contract ends ${item.contractEnd}`, time: '30 days' })); data.maintenance.filter(item => item.status !== 'Completed').forEach(item => result.push({ type: 'warning', icon: '!', title: `Unit ${item.unit} maintenance is ${item.status.toLowerCase()}`, detail: item.issue, time: item.priority })); data.utilities.filter(item => item.status !== 'Paid').forEach(item => result.push({ type: 'info', icon: 'i', title: `Unit ${item.unit} utility bill unpaid`, detail: `${item.type} · ${peso((item.current - item.previous) * item.rate)}`, time: 'Action needed' })); return result; }

const viewMap = { dashboard: renderDashboard, units: renderUnits, tenants: renderTenants, payments: renderPayments, expenses: renderExpenses, maintenance: renderMaintenance, utilities: renderUtilities, reports: renderReports, notifications: renderNotifications };
function navigate(view) { currentView = view; (viewMap[view] || renderDashboard)(); breadcrumbCurrent.textContent = label(view); document.querySelectorAll('.nav-item').forEach(item => item.classList.toggle('active', item.dataset.view === view)); document.getElementById('sidebar').classList.remove('open'); window.scrollTo({ top: 0, behavior: 'smooth' }); }
function setupSearch(type) { const input = document.getElementById('searchInput'); if (!input) return; input.addEventListener('input', () => { const query = input.value.toLowerCase(); const collection = data[type].filter(item => JSON.stringify(item).toLowerCase().includes(query)); const panel = document.getElementById('listPanel'); panel.querySelector('tbody').innerHTML = collection.length ? collection.map(item => `<tr><td>${escapeHtml(item.unit || item.name || item.date)}</td><td>${escapeHtml(item.issue || item.description || item.type || item.billing || '')}</td><td>${statusPill(item.status || item.category || '')}</td><td class="money">${peso(item.amount || item.rate || item.dueAmount || 0)}</td>${actions(type, item.id)}</tr>`).join('') : `<tr><td colspan="8" class="empty-note">No records found.</td></tr>`; }); }

function openModal(type, id = '') { const item = id ? data[type].find(record => String(record.id) === String(id)) : {}; const forms = { units: ['Unit record', `${field('id', 'Unit number', item.id, 'text', true)}${select('status', 'Status', ['available', 'occupied', 'maintenance'], item.status)}${field('rent', 'Monthly rent', item.rent, 'number', true)}${field('deposit', 'Security deposit', item.deposit, 'number')}${field('details', 'Unit details', item.details)}${field('amenities', 'Amenities', item.amenities)}${field('photo', 'Photo URL', item.photo)}${field('notes', 'Notes', item.notes)}`], tenants: ['Tenant profile', `${field('name', 'Full name', item.name, 'text', true)}${select('unit', 'Assigned unit', data.units.map(unit => unit.id), item.unit)}${field('phone', 'Contact number', item.phone)}${field('email', 'Email', item.email, 'email')}${field('emergency', 'Emergency contact', item.emergency)}${field('moveIn', 'Move-in date', item.moveIn, 'date')}${field('moveOut', 'Move-out date', item.moveOut, 'date')}${field('contractStart', 'Contract start', item.contractStart, 'date')}${field('contractEnd', 'Contract expiration', item.contractEnd, 'date')}${select('status', 'Tenant status', ['Active', 'Former', 'Pending'], item.status)}${field('notes', 'Notes', item.notes)}`], payments: ['Payment record', `${select('unit', 'Unit', data.units.map(unit => unit.id), item.unit)}${field('billing', 'Billing period', item.billing || 'August 2026', 'text', true)}${field('due', 'Due date', item.due, 'date')}${field('dueAmount', 'Amount due', item.dueAmount, 'number', true)}${field('paid', 'Amount paid', item.paid, 'number', true)}${field('date', 'Payment date', item.date, 'date')}${select('method', 'Payment method', ['Cash', 'GCash', 'Bank transfer', 'Other'], item.method)}${field('reference', 'Receipt / reference', item.reference)}`], expenses: ['Expense record', `${field('date', 'Expense date', item.date, 'date', true)}${select('category', 'Category', ['Electricity', 'Water', 'Internet', 'Repairs', 'Cleaning', 'Security', 'Supplies', 'Maintenance', 'Other'], item.category)}${field('description', 'Description', item.description, 'text', true)}${field('amount', 'Amount', item.amount, 'number', true)}${field('vendor', 'Vendor', item.vendor)}${select('method', 'Payment method', ['Cash', 'GCash', 'Bank transfer', 'Other'], item.method)}${field('reference', 'Receipt / reference', item.reference)}${field('notes', 'Notes', item.notes)}`], maintenance: ['Maintenance request', `${select('unit', 'Affected unit', data.units.map(unit => unit.id), item.unit)}${field('issue', 'Issue description', item.issue, 'text', true)}${field('reported', 'Date reported', item.reported, 'date', true)}${select('priority', 'Priority', ['Low', 'Medium', 'High'], item.priority)}${field('assigned', 'Assigned person', item.assigned)}${field('estimated', 'Estimated cost', item.estimated, 'number')}${field('actual', 'Actual cost', item.actual, 'number')}${select('status', 'Status', ['Reported', 'Pending', 'In Progress', 'Completed'], item.status)}${field('completed', 'Completion date', item.completed, 'date')}`], utilities: ['Utility reading', `${select('unit', 'Unit', data.units.map(unit => unit.id), item.unit)}${select('type', 'Utility type', ['Water', 'Electricity', 'Internet'], item.type)}${field('previous', 'Previous meter reading', item.previous, 'number', true)}${field('current', 'Current meter reading', item.current, 'number', true)}${field('rate', 'Utility rate', item.rate, 'number', true)}${field('billing', 'Billing period', item.billing || 'August 2026', 'text', true)}${select('status', 'Payment status', ['Unpaid', 'Paid', 'Partial'], item.status)}`] }; const form = forms[type]; document.body.insertAdjacentHTML('beforeend', `<div class="modal-backdrop" id="modal"><form class="modal" data-form-type="${type}" data-form-id="${id}"><div class="modal-header"><div><p class="eyebrow">Casa Java records</p><h2>${form[0]}</h2></div><button type="button" class="modal-close" data-close>×</button></div><div class="form-grid">${form[1]}</div><div class="modal-footer"><button type="button" class="secondary-button" data-close>Cancel</button><button class="primary-button">Save record</button></div></form></div>`); }
function collectForm(form) { const result = {}; new FormData(form).forEach((value, key) => { result[key] = value; }); ['rent', 'deposit', 'dueAmount', 'paid', 'amount', 'estimated', 'actual', 'previous', 'current', 'rate'].forEach(key => { if (key in result) result[key] = Number(result[key] || 0); }); if (form.dataset.formType === 'payments') { const balance = result.dueAmount - result.paid; result.status = result.paid <= 0 ? 'Unpaid' : balance > 0 ? 'Partial' : 'Paid'; } return result; }
function downloadCsv() { const rows = [['Type', 'Details'], ...data.payments.map(item => ['Payment', `Unit ${item.unit}, ${item.billing}, ${item.paid}`]), ...data.expenses.map(item => ['Expense', `${item.date}, ${item.category}, ${item.amount}`])]; const blob = new Blob([rows.map(row => row.map(value => `"${String(value).replaceAll('"', '""')}"`).join(',')).join('\n')], { type: 'text/csv' }); const link = document.createElement('a'); link.href = URL.createObjectURL(blob); link.download = 'casa-java-report.csv'; link.click(); URL.revokeObjectURL(link.href); showToast('CSV report downloaded.'); }
function showToast(message) { toast.textContent = message; toast.classList.add('show'); window.setTimeout(() => toast.classList.remove('show'), 2600); }

document.addEventListener('click', event => { const nav = event.target.closest('[data-view]'); const link = event.target.closest('[data-view-link]'); const add = event.target.closest('[data-add]'); const edit = event.target.closest('[data-edit]'); const remove = event.target.closest('[data-delete]'); const filter = event.target.closest('#filterButton'); if (nav) navigate(nav.dataset.view); if (link) navigate(link.dataset.viewLink); if (add) openModal(add.dataset.add); if (edit) openModal(edit.dataset.edit, edit.dataset.id); if (remove && confirm('Delete this record?')) { const recordIndex = data[remove.dataset.delete].findIndex(item => String(item.id) === String(remove.dataset.id)); data[remove.dataset.delete].splice(recordIndex, 1); save(); navigate(currentView); showToast('Record deleted.'); } if (filter) { const value = prompt('Type a status, category, or utility to filter:'); if (value !== null) { const input = document.getElementById('searchInput'); input.value = value; input.dispatchEvent(new Event('input')); } } if (event.target.closest('[data-close]')) document.getElementById('modal')?.remove(); if (event.target.closest('[data-export="csv"]')) downloadCsv(); if (event.target.closest('[data-export="pdf"]')) window.print(); });
document.addEventListener('submit', event => { const form = event.target.closest('[data-form-type]'); if (!form) return; event.preventDefault(); const type = form.dataset.formType; const values = collectForm(form); const id = form.dataset.formId; if (type === 'units') values.id = values.id || `U${Date.now()}`; else values.id = id || `${type.charAt(0)}${Date.now()}`; const index = data[type].findIndex(item => String(item.id) === String(id)); if (index >= 0) data[type][index] = { ...data[type][index], ...values }; else data[type].push(values); save(); form.closest('#modal').remove(); navigate(currentView); showToast('Record saved.'); });
document.getElementById('mobileMenu').addEventListener('click', () => document.getElementById('sidebar').classList.toggle('open'));
document.getElementById('notificationButton').addEventListener('click', () => navigate('notifications'));
renderDashboard();